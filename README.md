# 🧩 My Configs

Личные конфигурационные файлы для рабочего окружения macOS и серверов.  
Включают настройки редакторов, терминалов и автоматизацию через скрипты.

---

## 📁 Структура репозитория
.my-configs/
    ├── .vimrc      # Конфигурация Vim
    ├── ghostty/    # Настройки терминала Ghostty
    └── .setup/     # Скрипты для автоматического создания символических ссылок

---

## ⚙️ Установка

### 1. Клонировать репозиторий
```bash
git clone --no-checkout git@github.com:USERNAME/my-configs.git ~/my-configs
cd ~/.my-configs
git sparse-checkout init --cone
git sparse-checkout set .vimrc ghostty .setup

### 2. Создать символические ссылки
В папке .setup лежат скрипты, которые автоматически создают ссылки на нужные конфиги в домашней директории.
Пример выполнения:

bash ~/.setup/link.sh
сделай все обычным белым
